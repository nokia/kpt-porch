---
# SPDX-license-identifier: CC-BY-4.0
# Copyright The kpt Authors

matrix:
  - name: markdown
    dictionary:
      wordlists:
        - .wordlist.txt
    pipeline:
      - pyspelling.filters.markdown:
    sources:
      - '**/*.md'
    aspell:
      ignore-case: true
